import { LaTooltipDirective } from './tooltip.directive';

describe('LaTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new LaTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
